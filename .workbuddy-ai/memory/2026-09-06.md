# 2026-09-06

## 翻译 resources/CheatSheets 全部 md 为中文

- 38 个 md：30 个纯英文（CJK=0）需翻译，5 个已中文（angular/bash/chrome-devtools/cmd/conda）保持不动。
- 分 8 个并行 general-purpose agent 覆盖 30 个英文文件，覆盖原路径（已确认无代码引用 CheatSheets，安全）。
- 统一规则：自然语言/标题/表格说明列译中文；代码块、行内代码、命令、标识符、URL、占位符、emoji 原样保留；frontmatter `title` 改 `X 速查表`；TOC 链接文本译中文但 `#锚点` 不变，并在对应标题前插入 `<a id="原slug">` 保证跳转可用。
- 验收脚本（关键）：
  - 代码块保留校验：`re.findall(r"```.*?\n(.*?)```")` 对比英文备份与当前，拼接后相等即未改。
  - 锚点校验：TOC 的 `](#x)` 需有 `<a id="x"` 或标题 slug 匹配。
- 修复：Python/kotlin/react 三个文件 agent 改动了代码块（误删括号、加 `}`、译了代码注释、改坏 ASCII 表格边框），用「按位置从英文备份恢复代码块、保留正文与锚点」脚本修回；并补了 Python 两处缺失锚点（itertools、dataframe-plot-encode-decode）。
- 英文原版备份：`resources/CheatSheets_en_backup/`（未删除，待用户确认后清理）。
- 结论：最终 38 文件全部含中文，代码零差异，锚点全解析通过。
